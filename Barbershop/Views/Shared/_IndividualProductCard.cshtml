@model Barbershop.Models.Products

<style>
    .zoom{
        transition: transform .2s; 
    }
        .zoom:hover {
            transform: scale(1.2); 
        }
</style>

<div class="col-lg-4 col-md-6 pb-4 mt-3 filter @Model.ProductCategory.Name.Replace(' ', '_')">
    <div class="card h-100" style="border: none;">
        <div class="row g-0">
            <div class="col-md-5">
                <a asp-action="Details" asp-route-id="@Model.Id"><img src="@WC.ProductPath@Model.ProductImage" class="card-img-top h-100 img-fluid zoom" style="object-fit: cover; object-position: center;" alt="..."></a>               
            </div>
            <div class="col-md-7">
                <div class="card-body d-flex flex-column justify-content-between h-100">
                    <div>
                        <a asp-action="Details" asp-route-id="@Model.Id" style="text-decoration: none; color: black"><h5 class="card-title">@Model.ProductName</h5></a>                        
                        <p class="card-text">Ціна: <span class="text-dark h5">@Model.Price грн</span></p>
                    </div>
                    <div class="mt-3">
                        <a asp-action="Details" asp-route-id="@Model.Id" class="btn btn-dark" style="text-decoration: none; color:white; border-radius: 0px;">Детальніше</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
