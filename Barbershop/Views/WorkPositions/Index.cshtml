@model IEnumerable<Barbershop.Models.WorkPositions>


<div class="container p-5" style="background-color: #1F2029; margin-bottom: 30px">
    <h2 class="text-center" style="color:#FFEBA7">Посади</h2>
    <div class="row pt-4">
        <div class="text-center mb-4">
            <a asp-controller="WorkPositions" asp-action="Create" class="btn btn-primary">
                <i class="fas fa-plus"></i> &nbsp; Додати нову посаду
            </a>
        </div>
    </div>

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-responsive" style="width:100%; font-size:16px; border-color: #FFEBA7">
            <thead>
                <tr>
                    <th style="color:#FFEBA7">
                        Назва посади
                    </th>
                    <th style="color:#FFEBA7" class="text-center">
                        Редагування
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var obj in Model)
                {
                    <tr>
                        <td width="35%"><strong style="color:#FFEBA7">@obj.PositionName</strong></td>
                        <td wdith="35%" class="text-center">
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="WorkPositions" asp-route-Id="@obj.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                    <i class="fas fa-edit"></i> &nbsp;
                                </a>
                                <a asp-controller="WorkPositions" asp-route-Id="@obj.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                    <i class="fas fa-trash-alt"></i>

                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <h1 style="color:#FFEBA7">Посади тимчасово відсутні.</h1>
    }
</div>

